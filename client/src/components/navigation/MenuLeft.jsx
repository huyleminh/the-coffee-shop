import React from "react";
import PropsTypes from "prop-types";

MenuLeft.propsTypes = {
    menulist: PropsTypes.array
}

function MenuLeft(props) {
    return <div>Menu left for admin or employee layouts</div>;
}

export default MenuLeft;
